<br><div class="festival_show_page">
<h1><%= @festival.name %></h1>
<h2><%= @festival.start_date %> ---- <%= @festival.end_date %></h2>
<p> <%= @festival.description %><p>
<h4>Genre: <%= @festival.genre %></h4>
<h4>👍 <%= @festival.like %></h4>

 <%
=begin%>
 <%= link_to "👍", like_festival_path(@festival.id) %> 
 
<%
=end%>

<h3> LINE UP: <% @festival.bands.each do |band| %><h3>
        <%= link_to band.name, band %>
        <% end %>
  
<ul>
<h5>Check out below who is going to the festival: <% @festival.users.each do |user| %>
<li><%= user.first_name %> <%= user.last_name %></li>
<% end %>
</ul> 

<ul>
<h5>Reviews<%= @festival.reviews.each do |review| %>
<li><%= review.text %></li>
<% end %>
</ul>

<% if false %>
<h3>Users: <% @festival.users %>
<%= render(@users) || "There are noone who is going to the festival yet! Be first!" %>
<% end %>

<%= button_to "Leave Review", "/reviews/new" %>

<% if false %>
<h3>Review: <% @festival.reviews%>
<%= render(@users) || "There are no review for the festival. Let's write one!" %>
<% end %>
<div>